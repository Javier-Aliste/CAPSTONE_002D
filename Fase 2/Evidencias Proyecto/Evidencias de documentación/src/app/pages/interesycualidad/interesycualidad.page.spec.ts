import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InteresycualidadPage } from './interesycualidad.page';

describe('InteresycualidadPage', () => {
  let component: InteresycualidadPage;
  let fixture: ComponentFixture<InteresycualidadPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InteresycualidadPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
